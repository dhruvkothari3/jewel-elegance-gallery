import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jewels.jewelelegancegallery',
  appName: 'Jewel Elegance Gallery',
  webDir: 'dist'
};

export default config;
